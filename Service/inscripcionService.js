const Inscripcion = require('../models/inscripcion');

class InscripcionService {
  // Crear un evento
  static async listarInscripcion() {
    try {
      
    } catch (error) {
      console.error('Error a Inscripcion:', error);
      throw new Error('No seel Inscripcion');
    }
  }


  static async crearInscripcion() {
    try {
    
    } catch (error) {
      console.error('Error :', error);
      throw new Error('No s');
    }
  }

  static async actualizarInscripcion() {
    try {
      
    } catch (error) {
      console.error('Error :', error);
      throw new Error('Noo');
    }
  }

 
  static async eliminarInscripcion() {
    try {
      
    } catch (error) {
      console.error('Error :', error);
      throw new Error('No ');
    }
  }
}

module.exports = InscripcionService;
